/**
 * The following function will return the multiplication of the parameters
 * @param {number} a 
 * @param {number} b 
 */
const multiply = (a,b) => {
    return a*b;
}

module.exports = {
    multiply
};
